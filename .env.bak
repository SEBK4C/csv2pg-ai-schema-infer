# CSV2PG AI Schema Infer Configuration

############
# Gemini AI Configuration
############

# Required: Gemini API Key (for AI-powered schema inference)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Gemini Model Selection
# Current models (as of Jan 2025):
#
#   Latest/Recommended (auto-updates to newest):
#     - gemini-flash-latest         (Latest flash model - fast & balanced) [DEFAULT]
#     - gemini-pro-latest           (Latest pro model - best quality)
#
#   Production Models (stable versions):
#     - gemini-2.5-pro              (Best quality, largest context window)
#     - gemini-2.0-flash            (Fast and balanced)
#     - gemini-1.5-pro              (Mature, stable)
#     - gemini-1.5-flash            (Cost-effective)
#
#   Special Purpose:
#     - gemini-flash-lite-latest    (Lightweight, fastest, cheapest)
#     - gemini-2.0-flash-thinking-exp (Experimental reasoning model)
#
# To use a different model, uncomment and set:
# CSV2PG_LLM_MODEL=gemini-flash-latest

# Database Connection (Supabase/PostgreSQL)
# Default: Supabase local development setup
# For production, update these values before deployment
DATABASE_URL=postgresql://postgres:your-super-secret-and-long-postgres-password@localhost:5432/postgres

# Supabase Database Settings (for local Docker setup)
POSTGRES_HOST=localhost
POSTGRES_DB=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password

# Optional: Performance Tuning (auto-detected if not specified)
# CSV2PG_PERFORMANCE_WORKERS=8
# CSV2PG_PERFORMANCE_CONCURRENCY=4
# CSV2PG_PERFORMANCE_BATCH_SIZE=50MB
# CSV2PG_PERFORMANCE_WORK_MEM=512MB
# CSV2PG_PERFORMANCE_MAINTENANCE_WORK_MEM=2GB

# Optional: Sampling Configuration
# CSV2PG_SAMPLING_ROWS=100
# CSV2PG_SAMPLING_ENCODING=utf-8

# Optional: Chunking Configuration
# CSV2PG_CHUNKING_COLUMNS_PER_CHUNK=20
# CSV2PG_CHUNKING_PARALLEL_REQUESTS=true

# Optional: Advanced LLM Configuration
# CSV2PG_LLM_TIMEOUT=30              # API timeout in seconds
# CSV2PG_LLM_RETRY_ATTEMPTS=3        # Number of retries on failure
# CSV2PG_LLM_RETRY_DELAY=5           # Delay between retries in seconds

# Optional: Output Configuration
# CSV2PG_OUTPUT_DIRECTORY=./output
# CSV2PG_OUTPUT_DRY_RUN=false